# 人力與排班架構第一階段驗收紀錄

驗收日期：2026-08-02  
驗收環境：本機前端 + Supabase 開發分支 `dxmhswplkgkjzmqnukcb`  
正式環境：`wfhaqnicwqjfgzjcfmsq`，本階段未套用任何新 migration

## 結論

第一階段功能與架構驗收通過。開發分支維持 `parallel` 平行驗收模式，`cutover_month` 為空，既有正式資料與正式排班寫入路徑未切換。

## 已通過項目

- 人員類型、職稱、技能、原店與支援店歸屬已結構化。
- 正職與兼職預設工時、平假日工時及當日例外班次已分離。
- 同日多段班次、重疊阻擋與 15 分鐘輸入規則已完成。
- 標準班次模板已完成，總部維護、門店套用後可調整。
- 每 30 分鐘人力矩陣可顯示實際在班、有效人力、需求與缺口。
- 午峰 `11:00-14:00`、晚峰 `16:30-19:00` 已納入需求判斷。
- 人力需求變更採門店申請、總部核准後寫入規則。
- 跨店支援採申請、核准、自動寫入班表的閉環。
- 總部確認後鎖定；門店修改須限定日期、人員或班次申請。
- 薪資成本欄位已遮蔽；CEO/CFO 可看，COO 限時解鎖並留稽核紀錄。
- 新舊模組具單一寫入者保護，避免切換後重複或交錯寫入。
- 個人班表連結僅顯示本人班表資料。

## 驗證證據

- 自動測試：108/108 通過。
- Production build：通過。
- 桌機與手機版：無頁面水平溢出，無瀏覽器錯誤。
- 乾淨發布分支 `workforce-release-2026-08-02`：已從正式基礎重播，並含本階段全部 migration。
- 正式環境 migration：未含 2026-08-02 人力排班 migration。
- 開發資料：需求申請、薪資解鎖與排假稽核均無驗收測試殘留資料。
- 單一寫入者：直接寫入被阻擋，新模組標記寫入成功，測試 transaction 已 rollback。
- Supabase security advisor：0 個 ERROR；匿名角色無法存取薪資 RPC、角色 RPC 與 LINE 內部表。

## 發布分支修復紀錄

正式專案早期的基礎 schema、`target_monthly_revenue`、人員預設工時及排班鎖定表，曾以 SQL Editor 建立但未納入 migration 歷史，造成新建 Supabase 分支重播失敗。本次已在臨時發布分支補齊基礎後完成全歷史重播；正式資料庫未被修改。

臨時發布分支僅供驗收，驗收完成後刪除以停止計費。正式發布仍須另行核定，不得把「發布分支驗收通過」視為已上線。

## 切換限制

正式切換前仍須由管理者核定切換月份，再將開發分支 migration 合併至正式環境。未核定前不得操作「切換新版」，也不得直接在正式資料庫執行本階段 SQL。

## 已知非阻斷事項

- Supabase advisor 仍列出既有 `SECURITY DEFINER` RPC 與多重 permissive policy 警告；本階段新增 RPC 均在函式內執行角色檢查，且匿名權限已撤銷。
- Supabase Auth 的 leaked-password protection 目前未開啟，正式上線前建議於 Auth 設定啟用。
- 部分既有外鍵缺少索引，屬效能優化項，不影響本階段功能閉環。
